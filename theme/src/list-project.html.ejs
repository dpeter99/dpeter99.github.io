<main>
    <div>
        <header>
            <h1><%= meta.title %></h1>
        </header>
        <%- content %>

        <ul class="cards-list">
            <% pipeline.files.filter((d)=>helper.subArticle(d)).forEach(doc => {%>
                <project-card href="<%= helper.getLink(doc) %>">
                    <project-title title="<%= doc.meta.title %>" status="<%= JSON.stringify(doc.meta.stastatete) %>"></project-title>

                    <p slot="description"> <%= doc.meta.description %></p>

                </project-card>
                
            <% }) %>
        </ul>
    </div>
</main>